import { IngredientCategory } from './ingredient-category';

describe('IngredientCategory', () => {
  it('should create an instance', () => {
    expect(new IngredientCategory()).toBeTruthy();
  });
});
